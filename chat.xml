<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chatbot Interface</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* Chat Button Styles */
        #chat-button {
            position: fixed;
            bottom: 1.25rem;
            right: 1.25rem;
            background-color: #2563eb;
            color: white;
            width: 4rem;
            height: 4rem;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            z-index: 1000;
        }

        /* Chat Window Styles */
        #chat-window-container {
            display: none;
            position: fixed;
            bottom: calc(4rem + 1.25rem);
            right: 1.25rem;
            width: 320px;
            height: 480px;
            background-color: white;
            border-radius: 0.5rem;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            border: 1px solid #e5e7eb;
            flex-direction: column;
            overflow: hidden;
            z-index: 1001;
            transition: all 0.3s ease-out;
        }

        /* Chat Header Styles */
        #chat-header {
            background-color: #2563eb;
            color: white;
            padding: 0.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        #enlarge-button {
            background: none;
            border: none;
            color: white;
            cursor: pointer;
            font-size: 1rem;
            padding: 0.25rem 0.5rem;
        }

        /* Fullscreen Mode Styles */
        .fullscreen-chat {
            position: fixed !important;
            top: 0 !important;
            left: 0 !important;
            width: 100vw !important;
            height: 100vh !important;
            max-width: 100% !important;
            margin: 0 !important;
            border-radius: 0 !important;
            z-index: 9999 !important;
            right: 0 !important;
            bottom: 0 !important;
        }

        /* Chat Iframe Styles */
        #chat-iframe {
            border: none;
            flex-grow: 1;
            width: 100%;
            height: 100%;
        }

        /* Mobile Responsive Styles */
        @media (max-width: 640px) {
            #chat-window-container {
                width: 100vw !important;
                max-width: 100% !important;
                right: 0 !important;
                bottom: 70px !important;
            }
            
            .fullscreen-chat {
                height: 100vh !important;
            }
        }
    </style>
</head>
<body>
    <!-- Chat Button -->
    <div id="chat-button">
        <i class="fas fa-comment-dots"></i>
    </div>

    <!-- Chat Window Container -->
    <div id="chat-window-container">
        <div id="chat-header">
            <span>Chatbot</span>
            <button id="enlarge-button">
                <i class="fas fa-expand"></i>
            </button>
        </div>
        <iframe id="chat-iframe" 
                src="https://www.chatbase.co/chatbot-iframe/iKCQZZyJH0tBXVNCu0I-R"
                frameborder="0">
        </iframe>
    </div>

    <script>
        // DOM Elements
        const chatButton = document.getElementById('chat-button');
        const chatWindowContainer = document.getElementById('chat-window-container');
        const enlargeButton = document.getElementById('enlarge-button');
        let isFullscreen = false;

        // Toggle Chat Window
        chatButton.addEventListener('click', function() {
            chatWindowContainer.style.display = chatWindowContainer.style.display === 'flex' ? 'none' : 'flex';
        });

        // Fullscreen Toggle
        enlargeButton.addEventListener('click', function() {
            isFullscreen = !isFullscreen;
            
            if(isFullscreen) {
                // Store original dimensions
                chatWindowContainer.dataset.originalWidth = chatWindowContainer.style.width;
                chatWindowContainer.dataset.originalHeight = chatWindowContainer.style.height;
                chatWindowContainer.dataset.originalRight = chatWindowContainer.style.right;
                chatWindowContainer.dataset.originalBottom = chatWindowContainer.style.bottom;
                
                // Apply fullscreen
                chatWindowContainer.classList.add('fullscreen-chat');
                document.body.style.overflow = 'hidden';
            } else {
                // Restore original dimensions
                chatWindowContainer.classList.remove('fullscreen-chat');
                chatWindowContainer.style.width = chatWindowContainer.dataset.originalWidth || '320px';
                chatWindowContainer.style.height = chatWindowContainer.dataset.originalHeight || '480px';
                chatWindowContainer.style.right = chatWindowContainer.dataset.originalRight || '20px';
                chatWindowContainer.style.bottom = chatWindowContainer.dataset.originalBottom || '80px';
                document.body.style.overflow = '';
            }
            
            // Update icon
            const icon = enlargeButton.querySelector('i');
            icon.className = isFullscreen ? 'fas fa-compress' : 'fas fa-expand';
        });

        // Escape Key Support
        document.addEventListener('keydown', function(e) {
            if(e.key === 'Escape' && isFullscreen) {
                enlargeButton.click();
            }
        });

        // Click Outside to Close (when not fullscreen)
        document.addEventListener('click', function(e) {
            if(!isFullscreen && 
               !chatWindowContainer.contains(e.target) && 
               e.target !== chatButton) {
                chatWindowContainer.style.display = 'none';
            }
        });
    </script>
</body>
</html>
